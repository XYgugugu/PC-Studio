import React, { useEffect, useState } from "react";
import './CPU.css';

const CPU: React.FC<CPU> = () => {

    return (
        <div>
            <h2>CPU Details</h2>
            <p>This is the CPU page content.</p>
        </div>
    );
};

export default CPU;